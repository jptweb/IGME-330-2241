// @ts-check

/**
 * @type {number | string} value The value of the product.
 */
let price = 12 // or '12'

//this will cause an error in vscode! or red line
//price = []

/**
 * Adds two numbers together.
 * @param {number} a - First number
 * @param {number} b - Second number
 * @returns {number} Sum of a and b
 */
function add(a, b) {
  return a + b;
}

//Show how when I type to call it now it will show hints to what a and b are!

